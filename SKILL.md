---
name: math-modeling
version: 2.2.0
description: 当用户要求数学建模、建模竞赛、建模分析、代码求解、结果可视化或生成数学建模论文时使用。支持完整三阶段流程、单独执行任一角色，以及在关键节点使用独立 Subagent 进行阶段内质检；默认只启用质检 Subagent，用户可明确选择额外协作。
---

# 数学建模

本 Skill 用三个独立角色完成数学建模。生成的论文仅供用户参考，不作为可直接提交的作品。论文结构与格式必须以目标竞赛当届官方规则和官方模板为准，不能用往届经验替代官方要求。

## 决策优先级总闸（物理优先 · 价值仲裁层）

物理判据（能量守恒、量纲、口径一致性、预期范围、ρ/F/G、谷位差等）是**外部约束，不是算法输出**：能量守恒必须满足，不能由算法决定"是否满足"；两个可信口径不一致是数据本身的结构矛盾。本总闸是**价值仲裁层**，优先级**高于所有执行协议**：当总闸触发时，`references/温度策略.md` 第26条「禁止中途引入新模型思想」**自动失效**——是"被覆盖"，不是"例外"，以总闸的物理要求为准。设两级，均不阻塞、不等用户。

**第一级 · 前置预判（动笔前）**：`题目分析报告.md` 第10节「风险、反例与回退条件」必须写成决策树：列出求解中**最可能翻车的 ≥3 个点**，每个点写清 [触发条件 / 应对方案 / 失败出口]。**写不出失败出口的风险 = 无路可退的假设 = 自欺 → 阻断**，不进入求解，先补齐出口（出口可以是"承认无法辨识"）。预判摘要不进入论文正文，作为是否允许继续生成论文的内部信号。

**第二级 · 输出形态切换（矛盾出现时）**：第二级触发条件以 `题目分析报告.md` 第10节预判摘要中预先声明的可复核条件为准；未预判的客观矛盾信号一旦出现（如两个可信口径数值差异超过预判摘要阈值），直接切换并在分歧报告中标注"未预判触发"。出现结构矛盾（能量守恒违反、两个可信口径不一致、落在预期范围外、高阶解释无法闭环）时，**停止求解模式，自动切换分歧报告模式**，不等用户：不尝试求出单一最优解，把矛盾各方数值、各自证据、差异大小、为何无物理依据选择其一写进论文，形成完整「分歧报告」章节。默认句式：`这两个口径得到的结果不同，差异是 X%，我没有物理依据选择其一，因此报告为不确定区间；本模型不提供带内的唯一点估计`。

**两种完成形态，均为论文完成状态**：
- **「解决」幕**：高阶解释闭环成功（辨识性检查通过 + P6 三选二完成）→ 点估计 + 完备证据。
- **「分歧报告」幕**：闭环失败 → 区间 + 各方证据 + 承认无法辨识。**失败本身被输出为结论，不是未完成。**

**禁止**：
- 判据通过 ≠ 自动升级：判据数值是**呈堂证据**，不是**自动判决**。
- 全阶拟合失败 ≠ 回到低阶并宣称已解释：低阶与高阶同时报告为不一致，不合并为一个数。
- **不设"停机等用户"的阻断点**：停机只能发生在动笔前（补出口）或核验期（阻断并强制切分歧报告）；运行时一律自动切换输出形态。
- 一句话：**算法是探路的，模型是定案的。物理说不通时，不停在"选哪个方法"，而是把"选不出来"写进论文。**

## 强制执行协议

用户明确点名本 Skill 或任务命中本 Skill 时，严格执行以下协议；不要把它降级为建议：

1. 在首次进度更新中回显：已激活本 Skill、`SKILL_ROOT`、`PROJECT_ROOT`、当前阶段、目标竞赛与届次、计划读取的角色和工具入口。未确认的官方规则明确标为待核验。
2. 开始每个阶段前，实际读取该角色的 `SKILL.md`；使用 PDF、Excel、论文搜索、DOCX 或 LaTeX 时，再实际读取对应工具的 `SKILL.md`。知道文件路径不等于已经执行。
3. 严格调用 Skill 提供的脚本和模板。已有初始化、转换、编译或校验工具时，禁止为了省时手写替代实现。
4. 把任何校验预警视为未完成。只有当届官方规则或用户明确要求允许偏离时，才能记录“规则来源、偏离项、理由”后继续；不得自行降低篇幅、图表、公式、引用或编译质量目标。
5. 环境缺少引擎、搜索源、渲染器或依赖时，报告阻塞并继续完成仍可验证的部分；禁止静默换工具、跳过验证或用较差产物冒充完成。
6. 运行环境支持 Subagent 且任务会生成或修改固定交付物时，按 `references/Subagent调度.md` 在关键节点立即派发独立质检；禁止等全流程结束后才首次派发，作者自检不能替代独立验收。除固定质检外，不主动派发其他 Subagent；仅在用户明确启用具体协作任务时按所选范围执行。
7. 交付前运行当前角色规定的全部完成门禁。任一命令未运行、退出码非零、独立验收未通过、仍有未处理问题或产物在门禁后发生变化时，不得声称“已完成”。
8. 最终回复列出实际读取的入口、实际运行的关键命令、退出码、Subagent 门禁状态、核心质量指标和仍存在的阻塞；不得只说“已检查”。
9. **默认盲做**：全程按“实时参赛”处理——禁止检索官方评阅要点、参考答案、赛后解析及赛后发表的同题论文；文献依据只用竞赛年及更早。“官方规则”仅指格式、模板、篇幅、提交要求等硬性规定，不含评分要点。违反盲做视为未完成。
10. **模型—文献—数据三方交叉核验（本地 P9/P10/P11）**：模型选型、参数取值与结果判定必须形成三方自洽闭环——① 关键模型与参数有文献出处与适用波段声明，无文献值时显式声明「由数据反演，无独立依据」（本地 P9，挂 `M1`）；② 无文献依据的自由参数必须过「自由参数四问」，未过不得进模型，参数伦理字段入 `创新证据清单.json`（本地 P10，挂 `P2`）；③ 论文只允许来源自洽的模型组合以点估计进正文——残差最小不等于物理正确，模型选择必须有文献或物理依据（本地 P11，挂 `W2`）。本条第10条位于总闸之下：结构矛盾触发时总闸优先；2σ 等默认阈值可被题目精度/预判摘要覆盖，覆盖须记录理由。

## Subagent 默认策略与阶段门禁

- 默认模式只派发 `M1/P1/P2/W1/W2` 质检 Subagent；调研、原型和并行实验等可选协作默认关闭，不询问、不暂停流程。
- 用户可用自然语言启用一个或多个可选任务，也可随时关闭；未明确选择的类别保持关闭。
- 主 Agent 负责调度和权威产物写入；质检 Subagent 必须未参与被审产物的编写或修正，默认只读并返回证据。
- 完整流程依次通过 `M1` 建模终检、`P1` 最小可运行结果、`P2` 编程终检、`W1` 证据大纲和 `W2` 论文终检。`P1` 通过后才能全量计算与正式出图，`P2` 通过后进入论文规划，`W1` 通过后才开始长篇正文和双格式排版。
- `FAIL` 必须按证据返回对应角色修正并复验；被审产物发生实质变化时，相关 `PASS` 立即失效。
- 单阶段任务只运行对应门禁。环境不支持 Subagent 时，只能标记为 `BLOCKED` 或受限交付并明确报告“独立验收未完成”，不得把主 Agent 自检描述为独立通过或声称完整完成。

详细的默认开关、可选协作任务、派发输入、固定回执和并行边界见 `references/Subagent调度.md`。

## 发散—收敛温度策略（"调数"）

本 Skill 通过提示词控制每阶段的输出多样性与确定性，等效于调节 temperature，但**不修改、不声称修改任何会话 temperature 配置**（如外部 cc-switch/router 的 0.2）。因此不存在"临时改温度再还原"的配置动作，原始值从头到尾不变；同时禁止在任何进度、日志或完成声明中谎称"已将温度调到 X"。

- 本模块只负责"发散—收敛的**节奏与终点**"，发散内容（选什么模型、怎么创新）由 `references/roles/innovation-special/SKILL.md` 负责，不重复。
- 发散阶段（建模手）：按创新专项建立领域表示、数学表示和机制缺口，分轮生成结构实质不同的候选并主动寻找反例；候选数量不等于最终创新数量。
- 收敛阶段（编程手）：严格沿用已定方案，不引入新思想，以复现为先。
- 中发散阶段（论文手）：在已定模型与真实数据范围内多样化表达，禁止发明新模型或新结论。
- 每个发散任务结束、以及全流程结束时，必须回到默认收敛档，并在完成门禁中确认未改动任何 temperature 配置。

详细手法见 `references/温度策略.md`；进入相应阶段前按渐进式加载读取。

## 根目录契约

- `SKILL_ROOT`：本文件所在目录，只读。角色说明、算法资料、脚本和模板都从这里读取。
- `PROJECT_ROOT`：用户题目和项目所在目录，所有新产物只能写入这里。
- 两个根目录必须不同；`SKILL_ROOT` 完全只读，禁止写入或覆盖其中任何文件（无写入豁免）。
- 输入附件只读。确需修改模板时，先复制到 `PROJECT_ROOT` 再处理。

## 路由

| 用户意图 | 加载入口 | 是否要求前一阶段已完成 |
|---|---|---|
| 完整建模、完成整题 | 按"一键完整流程"执行（阶段0 题面拆解与必答清单前置 → 建模手 → 编程手 → 论文手） | 按顺序执行 |
| 只做题目分析、选模型 | `references/roles/建模手/SKILL.md` | 否 |
| 只写代码、跑结果、出图 | `references/roles/编程手/SKILL.md` | 需要题目和可执行的模型说明；缺失时先补齐必要分析 |
| 只写或修改论文 | `references/roles/论文手/SKILL.md` | 需要题目、模型、真实运行结果和图表；缺失时回退到对应阶段 |

不要在单阶段任务中强制执行完整流程。

## 一键完整流程（完整建模/完成整题：一次调用自动走全流程）

用户要求完整建模时，按以下**确定顺序**执行，主 Agent 不靠判断选读，逐阶段加载列出的参考：

**阶段 0 · 题面拆解与必答清单前置**（建模前，最高杠杆）
- 默认盲做：禁止检索官方评阅要点/参考答案/赛后解析/赛后同题论文；从题面自身拆解必答清单（映射到论文段落/证据/数值）→ 写入题目分析报告
- 加载：`建模手/references/题面拆解与必答清单前置.md`、`innovation-special/元能力.md`
- 自洽性检查：结果必须做自洽/复现/量纲一致性核查，且必须做合理性检查——外部锚点可用时执行归因审计，无可用锚点时做预期范围对照并注明无参考；合理性判定权在证据与外部约束，不在算法，无法闭环时输出分歧报告而非点估计

**阶段 1 · 建模手**（机制缺口→创新沙盒→模型）
- 加载：`建模手/SKILL.md` + `工作流程.md` + `建模设计理论.md` + `innovation-special/SKILL.md` + `innovation-special/references/创新发现工作流.md` + `references/温度策略.md`（高发散档）
- 必做：领域/数学双表示、机制缺口、分轮自由发散、反例和失效边界；物理几何候选按证明等级规划
- 产出：`题目分析报告.md`、`术语表格.md` → 门禁 `M1`

**阶段 2 · 编程手**（求解→对照→验证完备）
- 加载：`编程手/SKILL.md` + `工作流程.md` + `验证完备性.md` + `innovation-special/SKILL.md` + 按候选读取 `求解创新路由.md` 或 `物理几何简化.md` + `references/温度策略.md`（收敛档）
- 必做：先复现合理基线；仅原型化与题目瓶颈匹配的候选；用公平对照、证明、消融或边界测试决定 `VERIFIED/ADOPTED/DROPPED`
- 顺序：核心求解链最小切片 → 门禁 `P1` → 基线全量计算 + 创新原型与证据清单 + 候选证据池 + 图表论证清单 + 结果表 + 复现清单 → 创新审计 → 门禁 `P2`

**阶段 3 · 论文手**（证据映射→克制表达→评分自检）
- 加载：`论文手/SKILL.md` + `工作流程.md` + `章节模板.md` + `评分导向自检.md` + `innovation-special/SKILL.md`（只读取 `ADOPTED` 项）+ `文献规范.md` + `references/温度策略.md`（中发散档）
- 必做：摘要和正文中的创新主张逐项绑定真实产物；允许无核心创新；证据载体由论证决定，不强制配图或固定展示次数
- 顺序：证据大纲 → 门禁 `W1` → 长篇正文 → 完成门禁 → 门禁 `W2` → `完整论文.docx`

> 参考按阶段加载，任一阶段只读该阶段所需文件；复杂题（冲刺高分）全量执行，简单题可跳过阶段 0 与"复杂题必做"参考。

## 固定交付物

### 建模手

只交付：

1. `题目分析报告.md`
2. `术语表格.md`

### 编程手

交付：

1. 可运行的 Python（`.py`）或 MATLAB（`.m`）代码；可同时交付两种语言。
2. 代码运行结果表格，如 `.csv`、题目要求的 `.xlsx`。
3. 由真实模型、代码和结果生成的候选证据池。数据图使用 `raw_qN_*`、`process_qN_*`、`result_qN_*` 等可选来源标签；非数据图使用 `mechanism_qN_*`、`geometry_qN_*`、`flow_qN_*`、`architecture_qN_*` 或 `topology_qN_*`。Matplotlib 和 GeoGebra 2D 正式图同源输出 SVG 与至少 300 DPI PNG；GeoGebra 3D 保留 `.ggb` 源文件并输出高分辨率 PNG；复杂判定/算法流程图的 `.mmd` 作为可编辑源并同源导出 SVG 与至少 300 DPI PNG；不按题号或类别机械凑数。
4. `results/图表论证清单.json`，记录每个子问题的读者疑问、核心主张、证据载体、图的视觉角色、章节落点、前后论述和 `KEEP/MERGE/APPENDIX/DROP/PENDING_RENDER` 决策。
5. `results/复现清单.json`，含随机种子、输入文件 SHA-256、运行时和依赖版本、关键参数、唯一复现命令。
6. `results/创新证据清单.json`，允许 `items` 为空；记录创新候选状态、实现或证明、验证结果和失效边界，禁止为满足数量制造创新。

### 论文手

默认交付 Word 论文；用户显式要求时才生成 LaTeX 论文。

- Word：`完整论文.docx`；由 LaTeX 转换时同时保留哈希绑定的 `.conversion.json`。
- LaTeX（可选）：`完整论文-LaTeX/` 源码项目、由其实际编译得到的 `完整论文.pdf` 和哈希绑定的 `.build.json`。

用户明确要求 LaTeX 时，同时生成两种格式且正文内容、数据、图表和结论保持一致。内部检查记录不作为额外交付物。

论文图数由主张—证据映射决定，不设脱离题目结构的固定最低值。每个子问题至少有一个可追溯的正式证据单元，证据单元可以是图、表、关键公式组、代码输出或已核验文献。

## 视觉论证总契约

所有正式图以“补齐论证缺口”为生成单位，而不是以图型、题号或数量为生成单位。拟图前先登记读者疑问、核心主张、证据来源、视觉角色和章节落点；若表格、公式或文字能更直接完成论证，则不画图。

- 建模手在既有 `题目分析报告.md` 中提出视觉论证需求，不新增固定交付物。
- 编程手建立候选证据池和 `results/图表论证清单.json`。趋势、分布、误差、对比和收敛等数据图使用 `tools/figure/SKILL.md`；机理、几何、流程、架构和拓扑图使用 `tools/diagram/SKILL.md`。
- 数据图、架构图和拓扑图统一由 Python/Matplotlib 导出，NetworkX 只负责离散结构；复杂判定/算法流程图（判定树、多分支判据、算法流程）优先使用 Mermaid，以 `.mmd` 为可编辑源并同源导出 SVG 与至少 300 DPI PNG。连续二维/三维几何机理图优先使用 GeoGebra Classic；无法稳定自动渲染时输出可直接粘贴的逐行指令、参数清单和生成说明，由用户生成图片后继续门禁。Matplotlib 是几何图的自动后备，MATLAB 不进入正式配图路由。
- 论文手在 `W1` 中决定 `KEEP`、`MERGE`、`APPENDIX` 或 `DROP`，并把 `KEEP` 图写成“图前提出问题或判断依据、图中提供证据、图后陈述观察及其对模型结论的含义”的闭环。

详细门禁、清单结构、题型策略和放置逻辑见 `references/视觉论证与配图策略.md`。

## 阶段反馈闭环

- 任一 Subagent 门禁返回 `FAIL` 时，按回执中的文件、命令、数值或来源证据回到对应角色；修正后重跑受影响门禁，不得由主 Agent 口头覆盖。
- 编程手发现公式不完整、约束冲突或模型不可实现时，停止猜测，在对话中列出证据并回到建模手修正两个 Markdown 交付物。
- 论文手发现关键结论没有真实结果、图表或文献支撑时，回到编程手或建模手补齐，禁止编造。
- 修正后从被阻断阶段继续，不重复已通过的阶段。

## 建模约束

1. 先完整理解题目、附件、目标、约束和评价口径，再形成结论与模型方案。
2. 每道子问题最多使用两个独立模型体系。物理题中，同一控制方程或同一物理机制在不同近似阶次、边界条件或精度下的展开，计为一个模型族，不机械拆成多个模型；只有控制机理、状态变量或求解目标实质不同才另计模型。辅助的灵敏度分析、误差检验和可视化不计作独立模型。
3. 避免直接套用通用方法冒充创新；创新从题目特有机制、模型简化、物理几何证明、状态或决策重构、跨问统一、信息利用、算法结构或理论性质中产生。常规验证和可视化不自动计作创新。
4. 数据判定标准按题目、官方规则、领域文献或数据分析结果确定。
5. 不因两个模型得到相近结论就强制删除其中一个；是否保留由研究目的和证据价值决定。

## 渐进式加载

先读当前阶段的 `SKILL.md`，再按其中“何时加载”表读取所需参考，禁止一次性加载全部资料。

| 当前任务 | 额外读取 |
|---|---|
| 选模型或查算法 | `references/算法索引.md`，再读取一个或少数几个相关 `assets/*.md` |
| 进入发散/收敛/写作阶段（调数） | `references/温度策略.md` |
| 搜索论文 | `tools/paper_search/SKILL.md` |
| 读取题目 PDF | `tools/pdf/SKILL.md` |
| 处理 Excel | `tools/xlsx/SKILL.md` |
| 画趋势、分布、误差、对比、收敛等数据图 | `tools/figure/SKILL.md` |
| 画机理、几何、流程、架构或拓扑图 | `tools/diagram/SKILL.md`、`references/视觉论证与配图策略.md` |
| 生成 Word 论文 | `tools/docx/SKILL.md` |
| 生成 LaTeX 论文 | `tools/latex/SKILL.md` |
| LaTeX 论文转 Word | `tools/docx/SKILL.md` |
| 派发 Subagent 或阶段质检 | `references/Subagent调度.md` |

完整导航见 `references/README.md`。

## 完成判定

- 先满足“强制执行协议”，并提供可复核的门禁结果。
- 声称完整完成时，当前任务涉及的独立 Subagent 门禁均为 `PASS`，且通过后产物未发生未经复验的实质变化；缺少 Subagent 能力时只能报告 `BLOCKED` 或受限交付。
- 所有计算结论来自实际运行结果。
- `results/创新证据清单.json` 已通过创新审计；论文只表达 `ADOPTED` 项，允许没有采用项。
- 公式、表格和图表与代码结果一致。
- 引用可由 OpenAlex、AnySearch 或原始出版页面追溯。
- 论文已按目标竞赛当届官方规则配置，篇幅目标已经确认，每个子问题的正式证据覆盖和图表论证清单经过检查，图表编号与正文引用连续，参考文献与正文引用双向对应。Word 分支还须通过原生 OMML、DOCX 结构、转换警告门禁和渲染页数检查；LaTeX 分支还须通过环境诊断、真实编译、日志、权威资源—源码—PDF 哈希绑定、正文与附录页数、空白页、页面尺寸、字体嵌入和图片 DPI 检查。
- 所有产物位于 `PROJECT_ROOT`，`SKILL_ROOT` 未被改写。
- 每个关键模型满足模型—文献—数据三方交叉核验（强制执行协议第10条）：模型与参数有文献/波段出处，自由参数有物理依据或显式「由数据反演，无独立依据」声明，主结果只来自来源自洽的组合。
