---
name: math-modeling
description: 用于数学建模、竞赛分析、代码求解、结果可视化和建模论文。先完整读懂题意，再按任务需要进入建模、编程、论文或复查；不在开始时加载全部知识库和交付规范。
---

# 数学建模

本 Skill 保留“建模手 → 编程手 → 论文手”的生产链，但默认按需运行。先完整读懂题，再选模型和算法；审计只在真实风险出现时介入，不代替最初思考。生成论文仅供参考，格式以目标竞赛当届官方规则和用户模板为准。

## 四条不变量

1. 不静默改变题意、评价主体、对象范围、量词、聚合口径或边界行为。
2. 关键假设必须显式；题意存在多种合理解释时并列合同，用反例或对照裁决，不能把输出模板当目标函数。
3. 代理模型可用于搜索，最终结论必须由题意支持的严格判据验证。
4. 所有数值结论来自真实运行结果并可复现；证据不足时交付条件结论、区间或受限交付。

## 第一步：先建立全局故事

先耐心、连续地读完题目和各问之间的关系，再回读题目的总叙事、对象关系、量词和各问限制，直到能用自己的话重建整体机制；不要读一段就立刻填表、看结果模板或选择模型。此阶段不设时间、篇幅或步骤上限，宁可多次回读原题，也不因追求简短而过早收束。理解稳定后，再概括回答：

1. 最终被保护、评价、预测或优化的主体是什么？
2. 什么事件发生就意味着整体任务失败，什么条件才算整体成功？
3. 每一问是在推进该总目标，还是只要求一种局部输出或资源分配？

对用户的外显摘要可以很短，但内部读题和回读不应被压缩。普通题到此直接自由建模，无需输出卡片、反例或额外文件。只有局部公式、结果模板、分配字段或某一问的表述可能与全局故事脱节时，才打开 `references/roles/建模手/references/目标语义卡.md` 做短核对：局部对象是独立计分对象，还是共同目标的条件/资源？

若两种口径都会改变结论，才用一个最小反例和题面上下文裁决；不能确定时保留并行合同，不得静默选择。存在关键歧义时，在口径有依据前不冻结模型或开始正式计算。

## 按意图选择工作量

| 用户意图 | 默认执行 |
|---|---|
| 思路、读题、选模型、创新讨论 | 完整读题 → 全局故事 → `references/roles/建模手/SKILL.md`；不生成账本和论文交付物 |
| 写代码、求解、出图 | 先确认目标语义和可执行模型 → `references/roles/编程手/SKILL.md` |
| 写或修改论文 | 核对模型与真实结果 → `references/roles/论文手/SKILL.md` |
| 完整完成整题 | 题意冻结 → 建模手 → 编程手 → 论文手，允许按证据回环 |
| 复查、挑错、投稿/提交前终审 | 只加载命中风险的专项参考和相应门禁，不重跑无关流程 |

不要在单阶段任务中强制完整三阶段，也不要为普通思路讨论创建 JSON 账本、固定表格或 Subagent 门禁。

## 渐进式加载

先形成全局故事，再按信号读取参考；每次只读取能改变当前决策的最小材料：

| 信号 | 读取 |
|---|---|
| 问题含最优、最少、尽可能或方案竞争 | `references/竞争型问题协议.md` |
| 多执行器、重复对象、时序/分配/聚合表示 | `references/roles/innovation-special/references/决策空间完备性.md` |
| 覆盖、遮挡、保护、到达、阈值、延展对象或状态边界 | `references/roles/innovation-special/references/判据语义与边界审计.md` |
| 需要创新候选 | `references/roles/innovation-special/SKILL.md`，再按其路由读取一个相关参考 |
| 需要选择陌生或高影响算法 | `references/算法选择双源对照.md` → `references/算法索引.md` → 一个匹配的 `assets/*.md`；只在需要原文细节时精读 `knowledge/` 对应章节 |
| 需要领域依据、参数或适用域 | `tools/paper_search/SKILL.md` |
| 处理 PDF / Excel / 图 / Word / LaTeX | 只读取对应 `tools/*/SKILL.md` |
| 固定交付物的独立质检 | `references/Subagent调度.md` |

禁止一次性遍历或读取整个 `assets/`、`knowledge/`、全部角色和全部工具。算法知识库用于补盲，不代替独立建模。

## 文献介入时机

文献检索位于“独立初读之后、模型冻结之前”：

1. 先凭题面提出目标语义和模型候选，避免被已有方案锚定。
2. 再针对真实歧义、机理、参数和适用域检索竞赛年及更早的领域文献。
3. 根据证据采用、拒绝或保留并行合同，然后冻结模型。
4. 写作阶段只补题录核验和引用落点，不用结尾补文献反向证明既定模型。

默认盲做：不检索官方评阅要点、参考答案、赛后解析或赛后同题论文。用户明确提供同题材料用于赛后复盘时，可作为对照证据，但不能自动当作真值。

## 三角色与门禁

### 建模手

输出任务真正需要的分析；完整流程才固定生成 `题目分析报告.md` 和 `术语表格.md`。只有出现高影响语义风险时，`M1` 才先检查局部目标是否仍服务于全局故事；必要时才审查口径与反例。失败即停止，不继续检查算法、表格和格式。其余情况按实际触发项审查假设、自由度、文献和可实现性。

### 编程手

先完成核心求解链最小切片并执行 `P1`，再全量计算。达标型正确且可复现即可收敛；`competitive` 问题中首个可行解只建立 incumbent，随后按预设预算挑战。仅存在竞赛型问题时创建 `results/竞争性搜索账本.json`，并在 `P2` 前运行 `challenge_audit.py`。创新候选写入 `results/创新证据清单.json`，允许为空，但只有审计通过的 `ADOPTED` 项能进入论文。

### 论文手

只消费已冻结模型和真实结果。`P2` 通过后进入论文规划；`W1` 通过后才开始长篇正文；用户要求的格式完成并校验后执行 `W2`。不得在论文阶段发明模型、补造结果或把“当前最好已知”写成“全局最优”。

## 复查与独立质检

普通任务先自行检查题意、模型和结果的一致性。仅在完整交付、竞赛型搜索、用户要求复查，或存在高影响歧义时使用 `M1/P1/P2/W1/W2` 独立质检。`M1` 只优先审查已经识别出的语义风险；没有歧义不要求重写整张语义卡。禁止等全流程结束后才首次派发，发现 `FAIL` 应回到对应角色修正并复验。详细合同见 `references/Subagent调度.md`。

## 竞争与停止

- `conformance`：答案合同、正确性和复现闭合后可结束。
- `competitive`：首个可行解只建立 incumbent；用下界、精确小例、合理基线、Pareto 前沿或结构挑战建立竞争标尺。
- 只有已证最优、gap 达标、预设预算耗尽、不同结构路线边际收益不足或硬约束有证据阻断，才能停止竞争搜索。
- 未证明最优时报告“当前最好已知方案”、剩余 gap 或未知前沿。

## 路径与完成

- `SKILL_ROOT` 是本文件所在目录；执行建模任务时只读。用户明确要求维护本 Skill 时才可修改。
- `PROJECT_ROOT` 是题目项目目录；任务产物只写入这里，输入附件保持只读。
- Windows 文本读写显式使用 UTF-8，不能把 GBK 乱码当作事实。
- 完成的最低证据：题意合同一致、代码与公式一致、数值可复现、代理经严格判据认证、论文与结果同源；任一关键项未闭合时不得声称完整完成。

完整导航见 `references/README.md`。
