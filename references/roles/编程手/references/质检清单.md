# 编程手质检清单

- [ ] 只读取输入和建模手两个 Markdown 产物，未覆盖它们。
- [ ] 仅检查所选模型和功能需要的 Python 包或 MATLAB 工具箱；正式配图环境已检查 Python、Matplotlib、NumPy，涉及离散结构时再检查 NetworkX。
- [ ] Python/MATLAB 求解代码已真实运行，退出状态和异常已检查；正式配图统一由 Python/Matplotlib 生成。
- [ ] 约束、边界、量纲和验证指标正确。
- [ ] 在全量计算和正式出图前，独立质检 Subagent 已执行 `P1` 最小命令并返回 `PASS`。
- [ ] 随机种子和关键参数显式记录。
- [ ] 输入文件 SHA-256、版本和唯一复现命令已写入 `results/复现清单.json`。
- [ ] 绘图前已核对实际行列、表头、缺失、分组样本量、分布、异常值和单位；删行、聚合或抽样均有数量与理由。
- [ ] `results/图表论证清单.json` 已覆盖全部子问题；每个证据单元已有读者疑问、核心主张、证据来源、载体、章节落点和采用决策。
- [ ] `results/创新证据清单.json` 已存在；允许 `items` 为空，所有 `VERIFIED/ADOPTED` 项具有真实代码或证明、结果文件、复现命令、合理基线和失效边界。
- [ ] 已从题目分析报告提取全部 `completion_mode`；竞赛型的首个可行结果只作为 baseline/incumbent，没有被当作完成。
- [ ] 存在竞赛型子问题时，`results/竞争性搜索账本.json` 已记录硬约束、软目标、内部标尺、结构性挑战、incumbent、预算和合法停止证书。
- [ ] 竞赛型至少挑战过一个非参数微调的结构瓶颈；预算内高竞争价值候选已原型化，无证据候选未被主观 `DROPPED`。
- [ ] 每张候选图已有视觉角色、证据/面板映射、统计口径或结构语义、最终尺寸及图前/图后论述；图型与变量语义匹配。
- [ ] 每张图已选择叙事原型并声明主面板；不同证据权重或纵横比的面板未机械等分，互不支撑的结果未堆进同一画布。
- [ ] 已从题目分析报告列出全部子问题并规范为 `q1…qN`；每个子问题至少有一个正式证据单元，图、表、关键公式组或经核验文献均可。不存在为满足图数、题号、来源类别或图型多样性而生成的无效图。
- [ ] 机理、几何、流程、架构和拓扑图已使用 `tools/diagram/SKILL.md`；NetworkX 未被用于伪造连续几何，几何图优先使用 GeoGebra Classic；复杂判定/算法流程图优先使用 Mermaid，无法自动渲染时已交付 `.mmd` 源码与渲染说明并标记 `PENDING_RENDER`；MATLAB 未进入正式配图链。
- [ ] 无法自动渲染的 GeoGebra 图已输出 `.geogebra.txt` 指令和生成说明、Mermaid 流程图已输出 `.mmd` 源码与渲染说明，并在清单中标记 `PENDING_RENDER`；最终 `P2` 前已由用户生成并复审，或明确报告阶段性阻塞。
- [ ] 已按核心结论的真实风险选择验证；不含离散采样、网格、随机性或局部最优风险的模型未被强制添加无关验证。
- [ ] 使用采样、时间步或网格时已给出收敛序列、容差依据和决策影响；未套用固定档数或固定 1% 阈值。
- [ ] 随机模拟或算法已使用足够重复、区间、失败次数和决策翻转/后悔值；单个固定种子未被当作稳健证据。
- [ ] 边界和反例来自本题参数域、几何退化、容量、概率或决策阈值，最可能推翻结论的方向已经测试并记录。
- [ ] 物理几何简化已说明保持性质、证明等级、尺度或误差界；GeoGebra/Matplotlib 图没有代替证明。
- [ ] 图采用克制的 Nature/SCI 出版风格基线，无默认网格、装饰性 3D、`rainbow`/`jet` 色图或无说明的误导尺度；官方模板要求已优先应用。
- [ ] 同一变量跨图颜色语义一致，并使用色觉友好颜色及线型/标记等冗余编码。
- [ ] 第一视觉焦点是核心数据关系，不是长标题、大图例、密集纹理、冗余 colorbar 或大片空白；完整论述已移到图注。
- [ ] 稠密曲线未逐点使用大标记；图例采用直接标注、单个共享图例或合理分面，面板内没有相同语义的重复图例。
- [ ] 柱状图从零开始；少量点估计优先使用点图/区间图；离散或样本量不稳定的经验率没有被折线伪装成连续趋势。
- [ ] 标题、坐标轴、单位、图例、colorbar、数据范围和多面板编号正确；已标数值的 2×2 小矩阵没有冗余 colorbar，图例与标注不遮挡数据。
- [ ] 置信区间和统计量由代码计算，无硬编码 p 值；图注说明 n、中心统计量、区间/离散程度、检验和校正。
- [ ] SVG 与至少 300 DPI PNG 成对保存，SVG 文字可编辑，HTML 相对路径正确。
- [ ] 导出未用 `bbox_inches="tight"` 偷换最终尺寸；PNG 元数据中的实际英寸尺寸与图表契约一致。
- [ ] `python tools/figure/scripts/check_figure.py "<PROJECT_ROOT>/figures" --strict` 和带 `--manifest "<PROJECT_ROOT>/results/图表论证清单.json" --questions q1 q2 ... qN --strict` 的 `figure_audit.py` 已返回 0；审计结果未被人工忽略。
- [ ] `innovation_audit.py "<PROJECT_ROOT>/results/创新证据清单.json" --project-root "<PROJECT_ROOT>" --strict` 已返回 0；没有用手工说明覆盖失败。
- [ ] 若存在竞赛型子问题，`challenge_audit.py "<PROJECT_ROOT>/results/竞争性搜索账本.json" --project-root "<PROJECT_ROOT>" --questions <竞赛型题号...> --strict` 已返回 0。
- [ ] 已实际打开最终 PNG 和 `_qa` 灰度预览，在论文预计尺寸下用一句话复述第一视觉结论，并检查中文/符号缺字、裁切、重叠、灰度可辨、坐标截断、主次面板和跨面板一致性；问题已通过修改源代码后重跑解决。
- [ ] 所有新文件位于 `PROJECT_ROOT`，未写入 `SKILL_ROOT`。
- [ ] 代码、结果、图和复现清单冻结后，独立质检 Subagent 已执行 `P2` 并返回 `PASS`。
- [ ] `P1`、`P2` 通过后相关产物未发生未经复验的实质变化。
